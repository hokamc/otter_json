name: otter_json_generator
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^1.5.1
  build_runner: ^1.10.6
  analyzer: ^0.40.6
  source_gen: ^0.9.8
  glob: ^1.2.0

  otter_json:
    path: ../otter_json

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
